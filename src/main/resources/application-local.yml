# 로컬 개발용 실제 비밀값 (git에 커밋하지 않음, .gitignore에 등록됨)
# 기존에 application.yml에 하드코딩되어 있던 값은 이미 git 히스토리에 노출되었으므로
# 아래처럼 새 값으로 교체해서 사용하세요. (JWT secret은 새로 생성한 값으로 이미 바꿔뒀습니다.
# DB 비밀번호는 본인 MySQL root 계정 비밀번호를 실제로 변경한 뒤 그 값을 넣어주세요.)

jwt:
  secret: kYrXEkPUh6QNqFKv7yioLxHerLIFQ7ji6be594H4FIogisi07eWchn7vzBvYeVPg

spring:
  datasource:
    password: 2937